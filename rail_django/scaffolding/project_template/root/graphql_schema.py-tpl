"""
Compatibility shim for older discovery patterns.

Schemas are registered from RAIL_DJANGO_GRAPHQL_SCHEMAS in settings by default.
Add register_schema(registry) here only if you want code-driven registration.
"""
