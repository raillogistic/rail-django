"""
Optional schema registration hook.

By default, schemas are registered from RAIL_DJANGO_GRAPHQL_SCHEMAS in settings.
Add register_schema(registry) here only if you want code-driven registration.
"""
