# Table v3 Backend Release Notes

- Added static GraphQL contract for bootstrap, rows, actions, views, bulk edit, and exports.
- Added cache strategy, input validation, rate limiting, audit logging, and error taxonomy.
- Added management commands: `migrate_check`, `generate_v3_config`, `rollback_check`.
