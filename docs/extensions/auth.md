# Auth Extension

Module: `rail_django.extensions.auth`

- JWT login, refresh, logout, and optional register mutations.
- `me` query returns the current user with permissions and settings.
- Security and JWT settings live in [reference/security](../reference/security.md).
